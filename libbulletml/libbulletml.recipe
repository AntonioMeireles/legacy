# Gil Hamlin
# gil@foresightlinux.com
# 1st May 2005

class libbulletml(PackageRecipe):
    buildRequires = [ "glibc:devel" ]
    name = "libbulletml"
    version = "0.0.6"

    def setup(r):
        r.addArchive('http://shinh.skr.jp/libbulletml/%(name)s-%(version)s.tar.bz2')
        r.mainDir('bulletml/src')
#       r.Configure()
        r.Make()
#       r.MakeInstall()
        r.Install('libbulletml.a', '%(libdir)s/')

#       Here are the docs

#       r.Doc('README', 'AUTHORS', 'COPYING.FDL', 'COPYING.GPL', 'COPYING.LGPL', 'COPYING.Xiph')
