diff --git a/configure b/configure
--- a/configure
+++ b/configure
@@ -18871,7 +18871,11 @@
 	if test "x$libetpan_result" = "xyes"; then
 	   LIBETPAN_CPPFLAGS="`$libetpanconfig --cflags`"
 	   LIBETPAN_LIBS="`$libetpanconfig --libs`"
+	   LIBETPAN_STABLE=`$libetpanconfig --version | grep -v ^0`
 	   LIBETPAN_VERSION=`$libetpanconfig --version | sed "s/\.//g" | sed "s/-.*$//"`
+	   if test x"$LIBETPAN_STABLE" != "x"; then
+		LIBETPAN_VERSION="100"
+	   fi
 	   if test "$LIBETPAN_VERSION" -lt "057"; then
 		{ $as_echo "$as_me:${as_lineno-$LINENO}: result: *** Claws Mail requires libetpan 0.57 or newer. See http://www.etpan.org/" >&5
 $as_echo "*** Claws Mail requires libetpan 0.57 or newer. See http://www.etpan.org/" >&6; }
