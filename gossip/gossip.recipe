class gossip(PackageRecipe):

    buildRequires = [ 'loudmouth:devel' ] 
    name = "gossip"
    version = "0.8"
 
    def setup(r):
        r.addArchive('http://ftp.gnome.org/pub/GNOME/sources/gossip/0.8/gossip-%(version)s.tar.bz2')
        r.Configure()
        r.Make()
        r.MakeInstall()
