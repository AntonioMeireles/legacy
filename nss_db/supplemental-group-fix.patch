diff --git a/db-initgroups.c b/db-initgroups.c
index fcf4485..fc673ae 100644
--- a/db-initgroups.c
+++ b/db-initgroups.c
@@ -147,6 +147,9 @@ _nss_db_initgroups_dyn (const char *user, gid_t group, long int *start,
                  buffer = extend_alloca (buffer, buflen, newbuflen);
                  alloca_used += newbuflen;
                }
+             p = memcpy (line, value.data, value.size);
+             while (isspace (*p))
+               ++p;
            }
 
          if (err > 0 && grp.gr_gid != group)
