--- mkinitrd.orig  2008-04-16 08:01:39.000000000 -0400
+++ mkinitrd      2008-04-16 08:32:22.000000000 -0400
@@ -806,6 +806,9 @@ find_base_dm_mods()
 handlelvordev() {
     [ -n "$1" ] || return 0
     local vg=$(lvshow $1)
+    # if $1 is in /dev/mapper/VG-LV form, lvshow doesn't recognize it
+    [ -z "$vg" ] && vg=$(lvshow $(echo "$1" | \
+            sed 's/^\/dev\/mapper\/\(.*\)-\(.*\)/\/dev\/\1\/\2/'))
     if [ -n "$vg" ]; then
         vg=`echo $vg` # strip whitespace
         case " $vg_list " in
