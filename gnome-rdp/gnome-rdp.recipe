class GnomeRdp(PackageRecipe):
    buildRequires = [ 'desktop-file-utils:runtime', 'mono:runtime' ]
    name = "gnome-rdp"
    version = "0.1.2"


    def setup(r):
    	r.addArchive('%(name)s-%(version)s.tar.gz')
        r.Configure()
        r.Make()
        r.MakeInstall()
