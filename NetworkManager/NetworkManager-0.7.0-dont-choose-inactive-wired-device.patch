--- NetworkManager-0.7.0/src/NetworkManagerPolicy.c.dont-choose-inactive-wired-device	2006-05-28 16:02:48.000000000 -0400
+++ NetworkManager-0.7.0/src/NetworkManagerPolicy.c	2006-08-09 12:13:07.000000000 -0400
@@ -212,8 +212,10 @@
 			if (!(caps & NM_DEVICE_CAP_CARRIER_DETECT))
 				continue;
 
-			if (link_active)
-				prio += 1;
+			if (!link_active)
+				continue;
+
+			prio += 1;
 
 			if (nm_device_get_act_request (dev) && link_active)
 				prio += 1;
