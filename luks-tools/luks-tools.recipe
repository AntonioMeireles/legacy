loadRecipe('gnomepackage.recipe')
class LuksTools(GnomePackageRecipe):
 
    name = 'luks-tools'
    version = '0.0.12'
 
    buildRequires = ['cryptsetup:runtime', 'e2fsprogs:devel']
    extraConfig = '--disable-scrollkeeper'

    def unpack(r):
        r.addArchive("http://www.flyn.org/projects/luks-tools/")
