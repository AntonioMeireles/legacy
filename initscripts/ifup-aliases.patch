--- sysconfig/network-scripts/ifup-aliases.jau	2006-09-11 13:15:01.000000000 -0400
+++ sysconfig/network-scripts/ifup-aliases	2008-03-14 11:44:16.000000000 -0400
@@ -167,7 +167,7 @@
        IPGLOP="${ipa%%.*}_${ipb%%.*}_${ipc%%.*}_${ipc#*.}";
        DEVNUM=${DEVICE#*:}
 
-       if (LC_ALL=C; [[ ! "$DEVNUM" =~ '^[0-9A-Za-z_]*$' ]]); then
+       if (LC_ALL=C; [[ ! "$DEVNUM" =~ ^[0-9A-Za-z_]*$ ]]); then
 	      echo $"error in $FILE: invalid alias number" >&2
 	      return 1
        fi
