class UnRar(PackageRecipe):
    name = 'unrar'
    version = '3.5.4'
    
    buildRequires = [  ]   

    def setup(r):
	r.addArchive('ftp://ftp.rarlabs.com/rar/unrarsrc-3.5.4.tar.gz')
	r.Make('-f makefile.unix')
        r.Install('unrar', '%(bindir)s/')
