#! /bin/sh /usr/share/dpatch/dpatch-run
## 01_pangoxft.dpatch by  <Mike Hommey <glandium@debian.org>>
##
## All lines beginning with `## DP:' are a description of the patch.
## DP: patch to link against pangoxft with newer versions of pango.
## DP: pango code having been changed upstream, not filing a bug.

@DPATCH@


=== toolkit/library/Makefile.in
==================================================================
--- toolkit/library/Makefile.in	(revision 22)
+++ toolkit/library/Makefile.in	(local)
@@ -361,6 +361,10 @@
 EXTRA_DSO_LDOPTS += $(MOZ_CAIRO_LIBS)
 endif
 
+ifdef MOZ_ENABLE_PANGO
+EXTRA_DSO_LDOPTS += $(MOZ_PANGO_LIBS)
+endif
+
 export:: dlldeps.cpp dlldeps-obs.cpp
 
 dlldeps.cpp: $(topsrcdir)/xpcom/build/dlldeps.cpp
