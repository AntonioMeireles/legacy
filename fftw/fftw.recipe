# Gil Hamlin
# gil@foresightlinux.com
# 7th May 2005

class fftw(PackageRecipe):
    buildRequires = [ "glibc:devel" ]
    name = "fftw"
    version = "3.1.2"

    def setup(r):
        r.addArchive('http://www.fftw.org/%(name)s-%(version)s.tar.gz')
        r.Configure('--enable-float --enable-k7')
        r.Make()
        r.MakeInstall()

#       Here are the docs

        r.Doc('AUTHORS', 'COPYING', 'COPYRIGHT', 'INSTALL',
              'NEWS', 'README', 'TODO')
