#
# Copyright (c) 2004-2007 rPath, Inc.
# This file is distributed under the terms of the MIT License.
# A copy is available at http://www.rpath.com/permanent/mit-license.html
#

loadSuperClass('qtpackage')
class kdePackageRecipe(QtPackageRecipe):

    name = 'kdepackage'
    version = '20070212'

    buildRequires = [
        'acl:devel', 'alsa-lib:devel', 'arts:devel', 'attr:devel',
        'audiofile:devel', 'desktop-file-utils:runtime', 'esound:devel',
        'gamin:devel', 'glib:devel', 'jack:devel', 'kdelibs:devel',
        'libart_lgpl:devel', 'libfontenc:devel', 'libICE:devel', 'libidn:devel',
        'libmad:devel', 'libogg:devel', 'libSM:devel', 'libvorbis:devel',
        'libX11:devel', 'libXau:devel', 'libXcomposite:devel',
        'libXcursor:devel', 'libXdamage:devel', 'libXdmcp:devel',
        'libXext:devel', 'libXfixes:devel', 'libXft:devel', 'libXi:devel',
        'libXinerama:devellib', 'libxml2:runtime', 'libXmu:devel',
        'libXrandr:devel', 'libXrender:devel', 'libXScrnSaver:devel',
        'libXt:devel', 'libXtst:devellib', 'libXxf86misc:devel',
        'libXxf86vm:devel', 'Mesa:devel', 'nas:devel'
    ]

    abstractBaseClass = True

    def __init__(r, *args, **kwargs):

        QtPackageRecipe.__init__(r, *args, **kwargs)

        r.macros.kdehtmldir = '%(docdir)s/kde3/HTML'

    def setup(r):

        r.Environment('QTDOCDIR', '%(datadir)s/qt3/doc')
        r.Environment('kde_htmldir', '%(kdehtmldir)s')
        r.Environment('kde_confdir', '%(sysconfdir)s/kde3')

        r.extraConf += ' --enable-final --enable-new-ldflags '
        if r.macros.lib == 'lib64':
            r.extraConf += ' --enable-libsuffix=64 '

        r.unpack()
        if hasattr(r, 'postUnpack'):
            r.postUnpack()

        r.build()

        if hasattr(r, 'postInstall'):
            r.postInstall()

        if hasattr(r, 'policy'):
            r.policy()

        r.postPolicy()

    def unpack(r):

        pass

    def build(r):

        r.configure()
        r.make()
        r.makeinstall()

    def make(r):

        r.Make()

    def makeinstall(r):

        r.MakeInstall()

    def postPolicy(r):

        r.TagSpec('gtk-update-icon-cache', '%(datadir)s/icons/')
        r.TagSpec('desktop-file', '%(datadir)s/applications/kde/')

        r.CheckSonames(exceptions='%(libdir)s/libkdeinit_.*')
        r.RemoveNonPackageFiles(exceptions='.*\.la')
        r.DanglingSymlinks(exceptions='%(kdehtmldir)s/.*/.*/common')

