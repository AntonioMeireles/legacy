loadSuperClass('fptoolspackage')
class haddock(FpToolsPackageRecipe):
    name = 'haddock'
    version = '2.0.0.0'

    def unpack(r):
        r.addArchive('http://www.haskell.org/%(name)s/dist/')

    def makedocs(r):
        r.Make('html', dir='haddock/doc')
