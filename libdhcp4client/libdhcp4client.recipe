class libdhcp4client(PackageRecipe):
#class libdhcp4client(RedirectRecipe):
     name = 'libdhcp4client'
     version = '0foo'
#    version = '0'
 
     def setup(r):
#        r.addRemoveRedirect()
        string = 'x86'
        if not Arch.x86:
            string += '_64'
            
        r.Create('%(datadir)s/dummy',
                 contents = string)
#        r.addRemoveRedirect()
