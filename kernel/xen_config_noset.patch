--- arch/x86/kernel/cpu/common.c.orig	2011-11-01 16:29:21.000000000 -0400
+++ arch/x86/kernel/cpu/common.c	2011-11-01 16:31:45.000000000 -0400
@@ -302,9 +302,11 @@ static void __cpuinit filter_cpuid_featu
 				x86_cap_flags[df->feature], df->level);
 	}
 
+#ifdef CONFIG_XEN
 	/* RHEL Xen HVM guests must filter additional features. BZ#703055 */
 	if (xen_cpuid_base() == 0)
 		return;
+#endif
 
 	if (warn && cpu_has(c, X86_FEATURE_GBPAGES))
 		printk(KERN_WARNING
