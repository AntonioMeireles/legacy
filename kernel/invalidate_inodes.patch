diff -rupN linux-2.6.32-131.12.1.el6/fs/ntfs/super.c linux-2.6.32-131.12.1.el6.update/fs/ntfs/super.c
--- linux-2.6.32-131.12.1.el6/fs/ntfs/super.c	2011-07-31 16:03:50.000000000 -0400
+++ linux-2.6.32-131.12.1.el6.update/fs/ntfs/super.c	2011-10-19 12:13:24.000000000 -0400
@@ -3047,7 +3047,7 @@ iput_tmp_ino_err_out_now:
 	 * method again... FIXME: Do we need to do this twice now because of
 	 * attribute inodes? I think not, so leave as is for now... (AIA)
 	 */
-	if (invalidate_inodes(sb)) {
+	if (invalidate_inodes(sb, true)) {
 		ntfs_error(sb, "Busy inodes left. This is most likely a NTFS "
 				"driver bug.");
 		/* Copied from fs/super.c. I just love this message. (-; */
diff -rupN linux-2.6.32-131.12.1.el6/fs/smbfs/inode.c linux-2.6.32-131.12.1.el6.update/fs/smbfs/inode.c
--- linux-2.6.32-131.12.1.el6/fs/smbfs/inode.c	2009-12-02 22:51:21.000000000 -0500
+++ linux-2.6.32-131.12.1.el6.update/fs/smbfs/inode.c	2011-10-19 12:13:01.000000000 -0400
@@ -229,7 +229,7 @@ smb_invalidate_inodes(struct smb_sb_info
 {
 	VERBOSE("\n");
 	shrink_dcache_sb(SB_of(server));
-	invalidate_inodes(SB_of(server));
+	invalidate_inodes(SB_of(server), true);
 }
 
 /*
