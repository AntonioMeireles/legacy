loadSuperClass('kdepackage=foresight.rpath.org@fl:2-devel/20070212-9')
class konversation(kdePackageRecipe):

    name = 'konversation'
    version = '1.0.1'

    buildRequires = [
        'libxml2:runtime', 'perl-HTML-Parser:perl', 'perl:lib', 'ruby:runtime',
    ]

    def unpack(r):

        r.extraConf += ' --disable-final '

        r.addArchive('http://download2.berlios.de/konversation/')

