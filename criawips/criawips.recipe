loadSuperClass('gnomepackage.recipe')
class Criawips(GnomePackageRecipe):

    buildRequires = [ 'goffice:devel' ]

    name = 'criawips'
    version = '0.0.10.1'

    externalArchive = 'http://savannah.nongnu.org/download/%(name)s/%(name)s-%(version)s.tar.gz'
    extraConfig = '--disable-update-mimedb --disable-scrollkeeper --enable-gtk-doc'
