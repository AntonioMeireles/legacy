#
# Copyright (c) 2004-2006 rPath, Inc.
# Changes 2007 by Foresight Linux
# This file is distributed under the terms of the MIT License.
# A copy is available at http://www.rpath.com/permanent/mit-license.html
#

loadSuperClass('gnomepackage.recipe')
class Gstreamer(GnomePackageRecipe):
    name = 'gstreamer'
    version = '0.10.15'

    buildRequires = [ 'glib:devel', 'glib:runtime', 
                      'libxml2:devel', 'bison:runtime',
                      'popt:devel', 'gettext:runtime',
    # enabled 'check' - it builds gstreamer-check-0.10.pc
    'flex:runtime','check:devel', 'zlib:devel' ]

    # removed --disable-plugin-builddir  as not an option any more.
    # reenabled tests because otherwise our users wont get support.
    extraConfig  = ' --disable-examples'
    extraConfig += ' --with-cachedir=%(localstatedir)s/cache/gstreamer-%(majversion)s'

    if Use.builddocs:
        buildRequires.extend([ 'openjade:runtime', 'python:runtime',
                               'docbook-dsssl:runtime', 'docbook-xsl:data', 'netpbm:runtime',
                               'docbook-dtds:data', 'docbook-utils:runtime' ])
     # removed: --enable-docs-build as not an option any more.
        extraConfig += ' --with-html-dir=%(datadir)s/gtk-doc/html'

    def unpack(r):
        r.addArchive('http://gstreamer.freedesktop.org/src/%(name)s/')
        r.addSource('gstreamerplugin.tagdescription', macros=True)
        r.addSource('gstreamerplugin.taghandler', macros=True)
            
    def install(r):
        GnomePackageRecipe.install(r)
        r.Install('gstreamerplugin.tagdescription',
                  '%(tagdescriptiondir)s/gstreamerplugin')
        r.Install('gstreamerplugin.taghandler',
                  '%(taghandlerdir)s/gstreamerplugin', mode=0755)
        r.ComponentSpec('runtime', '%(libdir)s/%(name)s-%(majversion)s/')
        r.Provides(exceptions=['%(libdir)s/%(name)s-%(majversion)s/'])
        r.Remove('%(libdir)s/%(name)s-%(majversion)s/*.a')
        r.Requires('glib:devel', 
                   '%(libdir)s/pkgconfig/gstreamer-%(majversion)s.pc')
        r.Requires('libxml2:devel', 
                   '%(libdir)s/pkgconfig/gstreamer-%(majversion)s.pc')
