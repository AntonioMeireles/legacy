# Before someone ask me: this package is needed by libnjb and probably
# libmtp to let HAL make its magic and let the logged in user control
# their player

# Cesar Cardoso 

class setProcPerm(PackageRecipe):
    name="setprocperm"
    version="0.0"

    def setup(r):
	r.addSource('setprocperm')
	r.Install('setprocperm', '%(libexecdir)s/')
	r.SetModes('%(libexecdir)s/setprocperm', 0755)
