class HalSharp(PackageRecipe):

    buildRequires = [ 'hal:devel', 'mono:devel' ]

    name = 'hal-sharp'
    version = '20050719'
    def setup(r):
	r.addArchive('%(name)s.tar.bz2')
	r.mainDir('%(name)s')
	r.Run('./autogen.sh')
        r.Configure()
        r.Make()
        r.MakeInstall()
