class Muine(PackageRecipe):
    name = 'muine'
    version = '0.8.2'

    buildRequires = [ 'flac:devel', 'mono:devel',
			'gstreamer:devel', 'gtk-sharp2:devellib']

    def setup(r):
        r.addArchive('http://muine.gooeylinux.org/%(name)s-%(version)s.tar.gz')
        r.Configure('--enable-gstreamer')
        r.Make()
        r.MakeInstall()


