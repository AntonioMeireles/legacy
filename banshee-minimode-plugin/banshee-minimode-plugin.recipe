class BansheeMiniModePlugin(AutoPackageRecipe):

    buildRequires = [ 'banshee:lib', 'gtk-sharp:lib', 'mono:lib',
        'mono:runtime', 'pkgconfig:devel' ]

    name = 'banshee-minimode-plugin'
    version = '20060330'

    def unpack(r):
        r.addArchive('%(name)s-%(version)s.tar.bz2')
