class LaptopMode(PackageRecipe):

    name = 'laptop-mode'
    version = '0.1'

    def setup(r):
        r.addSource('ondemand.modules')
        r.Install('ondemand.modules', '%(sysconfdir)s/sysconfig/modules/ondemand.modules', mode=0755)
