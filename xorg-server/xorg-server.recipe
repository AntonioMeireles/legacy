loadSuperClass('xorgpackage.recipe=conary.rpath.com@rpl:devel')
class XorgServer(XorgPackageRecipe):
    name = 'xorg-server'
    version = '1.2.0'
    component = 'xserver'

    Flags.dmx  = True
    Flags.xnest = True

    # FIXME: Maybe this should become a Use flag shared with Mesa
    Flags.dri = True

    # FIXME: ld dies trying to link xorgcfg without libXext, need to make sure
    # configure checks for it
    # perl:lib needed to satisfy "perl: File::Glob" for /usr/bin/getconfig.pl
    buildRequires = [ 'bison:runtime', 'espgs:runtime',
        'flex:runtime', 'freetype:devel', 'libICE:devel',
        'libSM:devel', 'libXaw:devel', 'libXext:devel', 'libXfont:devel',
        'libXmu:devel', 'libXpm:devel', 'libXt:devel', 'libXxf86misc:devel',
        'libXxf86vm:devel', 'libfontenc:devel', 'liblbxutil:devellib',
        'libxkbfile:devel', 'libxkbui:devel',
        'linuxdoc-tools:runtime', 'ncurses:devel', 'perl:lib',
        'perl:runtime', 'xtrans:devel', 'zlib:devel',
        'xorg-sgml-doctools:data', 'libXfixes:devel', ]

    protoBuildRequires = [ 'bigreqsproto:devel', 'compositeproto:devel',
        'damageproto:devel', 'evieext:devel',  'fixesproto:devel',
        'fontsproto:devel', 'inputproto:devel', 'randrproto:devel',
        'recordproto:devel', 'renderproto:devel', 'resourceproto:devel',
        'scrnsaverproto:devel', 'trapproto:devel', 'videoproto:devel',
        'xcmiscproto:devel', 'xextproto:devel', 'xf86bigfontproto:devel',
        'xf86dgaproto:devel', 'xf86miscproto:devel', 'xf86vidmodeproto:devel',
        'xineramaproto:devel', 'xproto:devel' ]

    buildRequires.extend(protoBuildRequires)

    extraConfig = ' --disable-xprint'

    if Flags.dmx:
        buildRequires.extend([ 'libXi:devel', 'libXrender:devel',
            'libXres:devel', 'libXtst:devellib', 'libdmx:devel' ])
    else:
        extraConfig += ' --disable-dmx'

    if Flags.dri:
        buildRequires.extend([ 'Mesa:devel', 'glproto:devel', 'libdrm:devel',
            'xf86driproto:devel' ])

    if not Flags.xnest:
        extraConfig += ' --disable-xnest'

    def unpack(r):	

        srpm = 'http://download.fedora.redhat.com/pub/fedora/linux/core/development/source/SRPMS/xorg-x11-server-1.2.99.903-2.fc7.src.rpm'
    
        r.macros.osname = "'Foresight Linux (rPath Linux derivative)'"
        r.macros.vendor = "'António Meireles aka doniphon'"
        r.extraConfig += ' --with-os-name=%(osname)s --with-vendor=%(vendor)s'
        r.extraConfig += ' --enable-install-libxf86config'
        r.extraConfig += ' --with-fontdir=%(datadir)s/X11/fonts --with-default-font-path=unix/:7100,built-ins'
        r.extraConfig += ' -enable-xvfb  --enable-kdrive --enable-xephyr'

        XorgPackageRecipe.unpack(r)
        
        r.addSource('xserver.pamd',
                    dest='%(sysconfdir)s/pam.d/xserver', mode=00644)
        
        # according to http://xorg.freedesktop.org/wiki/ModularDevelopersGuide
        # we need the Mesa source to build GLX support
        r.macros.mesaVersion = '6.5.2'
        r.addArchive('mirror://sourceforge/mesa3d/MesaLib-%(mesaVersion)s.tar.bz2', use=Flags.dri)
        
        if Flags.dri:
            r.extraConfig += ( ' --enable-dri'
                               ' --enable-glx-tls'
                               ' --with-mesa-source=%(builddir)s/../Mesa-%(mesaVersion)s'
                               ' --with-dri-driver-path=%(libdir)s/dri'
                               ' --enable-composite'
                               ' --enable-xtrap'
                               ' --enable-xcsecurity'
                               ' --enable-xevie')
        else:
            r.extraConfig += ' --disable-dri'

        r.addPatch('xorg-x11-server-1.1.1-always-mouse-thyself.patch')

# general bug fixes
        r.addPatch('xorg-x11-server-0.99.3-init-origins-fix.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-libxf86config-dont-write-empty-sections.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.1-builderstring.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.1-xkb-in-xnest.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.1-vbe-filter-less.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.1-vt-activate-is-a-terrible-api.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.1-graphics-expose.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.1-automake-1.10-fixes.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.1-glcore-visual-matching.patch', rpm=srpm)
        
# OpenGL compositing manager feature/optimization patches.
        r.addPatch('xorg-x11-server-1.1.0-no-move-damage.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.0-dont-backfill-bg-none.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.2.0-enable-composite.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.1-no-composite-in-xnest.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.1-offscreen-pixmaps.patch', rpm=srpm)
        
        # Red Hat specific tweaking, not intended for upstream
        # XXX move these to the end of the list
        r.addPatch('xorg-x11-server-Red-Hat-extramodes.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.2.0-xephyr-only.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.0.1-fpic-libxf86config.patch', rpm=srpm)
#        r.addPatch('xorg-x11-server-1.2.0-selinux-awareness.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.1-builtin-fonts.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.1-no-scanpci.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.1.1-spurious-libxf1bpp-link.patch', rpm=srpm)
        r.addPatch('xorg-x11-server-1.2.0-xf86config-comment-less.patch', rpm=srpm)
        
        r.addPatch('xorg-x11-server-1.2.0-maxpixclock-option.patch', rpm=srpm)

        r.addPatch('xserver-1.2.0-geode-mmx.patch', rpm=srpm)
        r.addPatch('xserver-1.2.0-xephyr-keysym-madness.patch', rpm=srpm)
        r.addPatch('xserver-1.2.0-vfprintf.patch', rpm=srpm)

        # X.Org security advisory, April 3rd, 2007
        # Multiple vulnerability in X server, libXfont and libX11
        # CVE IDs: CVE-2007-1003 CVE-2007-1351 CVE-2007-1352 CVE-2007-1352
        # (FL-223)

        r.addPatch('http://xorg.freedesktop.org/archive/X11R7.2/patches/xorg-xserver-1.2.0-xcmisc.diff')


    def policy(r):
        r.SetModes('%(bindir)s/Xorg', 04611)

        # FIXME: copied from xorg-x11 recipe; is this for consolehelper?
        r.Create('%(sysconfdir)s/security/console.apps/xserver')

        # make xorg.conf an InitialContents file so it's managed locally, but
        # removed when xorg-server is
        r.Create('%(sysconfdir)s/X11/xorg.conf')
        r.InitialContents('%(sysconfdir)s/X11/xorg.conf')

        # Install the vesamodes and extramodes files to let our install/config tools
        # be able to parse the same modelist as the X server uses (rhpxl).
        r.Install('hw/xfree86/common/vesamodes','%(datadir)s/xorg/vesamodes')
        r.Install('hw/xfree86/common/extramodes','%(datadir)s/xorg/extramodes')

        # contrary to ATI, only nVidia messes with files from xorg-server
        if Use.nvidia:
            r.Remove('%(libdir)s/xorg/modules/extensions/libglx.*')
            



