class Enca(AutoPackageRecipe):
    name = 'enca'
    version = '1.9'

    buildRequires = ['perl:runtime', 'recode:runtime', 'glibc:locale', 'gtk-doc:runtime', 'recode:devel']

    def unpack(r):
        r.addArchive('http://www.sfr-fresh.com/cgi-bin/warex?unix/privat/enca-1.9.tar.bz2')
