#
# Copyright (c) 2010 Foresight Linux
# This file is distributed under the terms of the MIT License.
# A copy is available at http://www.rpath.com/permanent/mit-license.html
#

class FaenzaIconTheme(PackageRecipe):
    name = 'faenza-icon-theme'
    version = '0.8'

    # URL: http://gnome-look.org/content/show.php/Faenza?content=128143

    buildRequires = ['unzip:runtime']

    def setup(r):
        r.unpack()
        r.install()
        r.policy()

    def unpack(r):
        r.addArchive('http://www.deviantart.com/download/173323228/faenza_icons_by_tiheum-d2v6x24.zip',
            dir='%(name)s-%(version)s')
        r.addArchive('Faenza-Dark.tar.gz', sourceDir='.', dir='%(name)s-%(version)s')
        r.addArchive('Faenza.tar.gz', sourceDir='.', dir='%(name)s-%(version)s')
        r.addArchive('emesene-faenza-theme.tar.gz', sourceDir='.', dir='%(name)s-%(version)s')

    def install(r):
        r.Install('Faenza', '%(datadir)s/icons/')
        r.Install('Faenza-Dark', '%(datadir)s/icons/')
        # theme for emesene, MSN client
        r.Install('emesene', '%(datadir)s/')

        r.SetModes('%(datadir)s/icons/*/index.theme', 0644)

    def policy(r):
        place = '%(datadir)s/icons/Faenza/places'
        # use fedora logo
        r.Symlink('%s/scalable/distributor-logo-fedora.svg' % place,
                  '%s/scalable/distributor-logo.svg' % place)
        r.Symlink('%s/scalable/start-here-gnome.svg' % place,
                  '%s/scalable/start-here.svg' % place)
        for x in ('16', '22', '24', '32', '48'):
            r.Symlink('%s/%s/distributor-logo-fedora.png' % (place, x),
                      '%s/%s/distributor-logo.png' % (place, x))
            r.Symlink('%s/%s/start-here-gnome.png' % (place, x),
                      '%s/%s/start-here.png' % (place, x))

        place = '%(datadir)s/icons/Faenza-Dark/places'
        # use fedora logo
        r.Symlink('%s/scalable/start-here-gnome.svg' % place,
                  '%s/scalable/start-here.svg' % place)
        for x in ('16', '22', '24', '32', '48'):
            r.Symlink('%s/%s/start-here-gnome.png' % (place, x),
                      '%s/%s/start-here.png' % (place, x))
