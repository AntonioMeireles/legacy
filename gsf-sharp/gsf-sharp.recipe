class GsfSharp(PackageRecipe):

    buildRequires = [ 'gtk-sharp:devel', 'mono:devel', 
			'gtksourceview:devel' ]

    name = 'gsf-sharp'
    version = '0.1.0'
    def setup(r):
        r.addArchive('%(name)s-%(version)s.tar.bz2')
        r.Configure()
        r.Make()
        r.MakeInstall()
