class popularity(PackageRecipe):
    name = 'popularity'
    version = '0.1'

    def setup(r):
        r.addSource('popularity.cron')
        r.Install('popularity.cron','/etc/cron.daily/popularity')
	
