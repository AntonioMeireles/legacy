--- a/usr/lib/site_ruby/aes/amiutil/http.rb	2006-12-28 09:11:44.000000000 -0500
+++ b/usr/lib/site_ruby/aes/amiutil/http.rb	2007-02-20 15:29:13.586463988 -0500
@@ -36,7 +36,7 @@
       curl_arguments += " --max-filesize #{max_file_size}"
     end
     
-    cmd_line = "curl -f -s #{curl_arguments} #{url} | tee #{path} | openssl sha1; exit ${PIPESTATUS[0]}"
+    cmd_line = "curl -k -f -s #{curl_arguments} #{url} | tee #{path} | openssl sha1; exit ${PIPESTATUS[0]}"
         
     calculated_digest = IO.popen( cmd_line ) { |io| io.readline.chomp }
     
@@ -83,7 +83,7 @@
       curl_arguments = ""
     end
         
-    cmd_line = "curl -f -s #{curl_arguments} #{url} -T #{path}"
+    cmd_line = "curl -k -f -s #{curl_arguments} #{url} -T #{path}"
     system( cmd_line )
     
     unless $?.exitstatus == 0
@@ -111,7 +111,7 @@
       curl_arguments = ""
     end
     
-    cmd_line = "curl -f -s -X DELETE #{curl_arguments} #{url}"
+    cmd_line = "curl -k -f -s -X DELETE #{curl_arguments} #{url}"
     # @log.info { "HTTP curl command line: #{cmd_line}" }
     retval = system( cmd_line )
     raise Error::Transfer.new( "delete failed for #{url}") unless retval
