#!/bin/bash
# FIXME hardcoding the GTK style isn't fair to now gnome users. fix it
LD_PRELOAD=/opt/skype/libgcc_s.so.1:/opt/skype/libstdc++.so.6 /opt/skype/skype.bin --disable-cleanlooks -style GTK $@ 
