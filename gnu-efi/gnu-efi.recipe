class GNUEFI(CPackageRecipe):
    name = 'gnu-efi'
    version = '3.0m'
    def setup(r):
        r.addArchive('mirror://sourceforge/%(name)s/%(name)s_%(version)s.orig.tar.gz')
        r.addPatch('efibind_h.patch')
        r.Make()
        r.MakeInstall('INSTALLROOT=%(destdir)s/%(prefix)s')
