--- udev-069/start_udev.old	2005-12-07 18:00:54.000000000 -0500
+++ udev-069/start_udev	2005-12-07 18:01:55.000000000 -0500
@@ -73,9 +73,9 @@
 
 	[ -d $udev_root/pts ] || mkdir -m 0755 $udev_root/pts
 	[ -d $udev_root/shm ] || mkdir -m 0755 $udev_root/shm
-	[ -a /dev/MAKEDEV ] || ln -s $MAKEDEV /dev/MAKEDEV;
 
 	if [ -x $MAKEDEV ]; then
+        	[ -a /dev/MAKEDEV ] || ln -s $MAKEDEV /dev/MAKEDEV;
 		for i in /etc/udev/makedev.d/*.nodes; do
 			if [ -f "$i" ]; then 
 				cat "$i" | sed -e 's,#.*,,g' | \
