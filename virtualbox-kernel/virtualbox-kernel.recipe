#class GnomeCompizManager(RedirectRecipe):
class VBK(PackageRecipe):
#class VBK(RedirectRecipe):
     name = 'virtualbox-kernel'
     version = '0foo'
#    version = '0'
 
     def setup(r):
#        r.addRemoveRedirect()
        string = 'x86'
        if not Arch.x86:
            string += '_64'
            
        r.Create('%(datadir)s/dummy',
                 contents = string)
#        r.addRemoveRedirect()
